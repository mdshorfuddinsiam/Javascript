const ages = [];
const numbers = [40, 34, 89, 300, 12, 9, 75];

console.log(numbers);
const out1 = numbers.pop();
console.log(numbers);
const out2 = numbers.pop();
// numbers.pop(300);    // not working
console.log(numbers);
console.log(out1, out2);
